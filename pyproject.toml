[tool.ruff]
target-version = "py312"
line-length = 120
extend-exclude = [
  "build",
  "dist",
  "release",
  "venv",
]

[tool.ruff.lint]
select = [
  "E9",
  "F",
  "C90",
]

[tool.ruff.lint.mccabe]
max-complexity = 122
