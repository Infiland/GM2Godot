# Project Instructions

- Before considering code changes complete, run `./venv/bin/pyright --warnings` and fix every reported error or warning.
- Do not leave new Pylance/Pyright diagnostics, lint errors, or warnings in changed code.
- Run the relevant tests after lint/type-check fixes. For broad changes, run `./venv/bin/python -m unittest`.
