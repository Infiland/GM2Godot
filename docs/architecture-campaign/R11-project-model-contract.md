# R11 accepted implementation contract

Status: ACCEPTED and assigned by root after independent and root contract review. Fixed implementation parent C is `ec257912ec907161b2e89d552a76dc591ed33b68`. The final source/import/metric refresh is verified in `/Users/infi/Documents/Github/.gm2godot-v080-evidence/R11/final-C-entry-source-confirmation.json`. This entry supersedes historical proposal status/source and completed-R04 implementation-start wording below; all substantive API, behavior, scope, size and proof requirements remain binding.

Sole implementer: `audit_gml_resources` in `/Users/infi/Documents/Github/GM2Godot-080-project-model`. Independent actual-code reviewer: `audit_policy_tests_docs`, then root. The conditional start follows [R04's reviewed entry contract](R04-parallel-entry-contract.md). Cleanup PR #883 and CI `33943223988` are pending; no child PR or campaign integration is authorized before successful exact cleanup PR and merge proof. If cleanup changes or fails, root reassesses affected work and invalidates stale evidence.

Root has accepted the original finite contract SHA256 `4052f2e8aba9183ea2aa54cafac0b13c06e2cadc4900f07b0b2f3ae66f7937e6` and confirmed all 12 source inputs at C. Applicable current measurements/import projections are recorded in the external final source receipt; historical measurement tables below remain provenance where earlier source counts differ. Strict comparison uses immutable C; policy thresholds, retained evidence, scopes and suppressions may not grow.

The complete allowed-file inventory below includes `maintainability-baseline.json` for integration only. **Root is its sole writer during parallel work.** The implementation owner edits only the other allowed paths, reports measured reductions, and asks root to run any baseline update sequentially before the final strict gate. Root also owns campaign documents, coverage-policy and architecture-verification integration. Do not edit, commit or push those shared files. Do not commit or push implementation source before root code approval and explicit instruction.

Freeze source before proof. Coordinate a reserved CPU window with root before full-suite or performance/parity runs; focused checks and before-production characterization may proceed. Use the approved native Python 3.12.10 environment and exact Godot `4.7.2.stable.official.ed1daf0bf`, all five pinned project environments where required, and preserve every native/skip distinction in the finite specification.

Root integration adds `src/conversion/project_model.py` to the existing project-parsing coverage cohort with unchanged 92.28 line / 82 branch floors and all existing paths retained. The owner does not edit coverage policy.

Task / issue: R11 / #797.

Problem and benefit: the production GameMakerProjectManifest records share a 1091-physical/1984-structural parser owner; project options have two decoding paths; the registry interprets two raw audio fields; resource_models.ProjectModel is a duplicate projection with no production aggregate-parser callers. Give these project records one canonical owner, use R10 recursive JSON throughout the project parser, share the actual options parser, and remove those exact duplicates. This does not complete #797 or remove deferred family raw reads.

Current owners: project_manifest.py records/parser; project_settings.py fresh source validation and operation lifecycle; asset_registry.py audio load/gain projections; resource_models.py descriptive ProjectModel. Proposed owners: project_model.py owns records and three specific derived properties; project_manifest.py owns acquisition/parsing/diagnostics and stable type exports; existing consumers retain their operation/writer responsibilities.

## Exact data and public contract

Move exactly these eight frozen dataclasses, with their existing methods, field order and defaults: ProjectResourceReference, ProjectConfigOverride, ProjectConfiguration, ProjectOption, ProjectTextureGroup, ProjectAudioGroup, ProjectIncludedFile, GameMakerProjectManifest. project_manifest.py retains all eight as explicit direct identity-preserving imports using the same exported names. It retains its loader and unsupported-option diagnostic service. No second definitions, subclasses, forwarding functions, export registry, new __all__, or mass migration of the 17 production importers.

Change project record raw_data annotations from legacy JsonDict to JsonObject and option/override value annotations from object to JsonValue. Preserve container references and order, empty factories, frozen field assignment, shallow mutability and existing query results. Do not add constructor validation, copies, deep freezing, finite-number filtering or lazy reparsing. Direct constructors remain usable; callers injecting data own its declared JsonValue contract. Runtime behavior for existing injected values is not newly validated at construction. A raw dictionary mutation does not rebuild already-parsed option/group tuples.

Move the one empty-object factory to private project_model._empty_json_object, and move the path helper to public project_model.normalize_project_manifest_path. Parser and query callers use that same helper; delete the old definition, with no private cross-owner imports or compatibility helper.

Add frozen ProjectOptionsMetadata with exactly platform: str, source_path: str, source: str, raw_data: JsonObject and options: tuple[ProjectOption, ...], in that field order. All fields are required. source_path/source are the unmodified GameMakerJsonDocument strings; raw_data is the same validated root object. Each ProjectOption value retains the corresponding JSON value reference and source-location policy. A valid empty object gives an empty options tuple; a nonobject root returns no record.

Add exactly one field to GameMakerProjectManifest: option_files: tuple[ProjectOptionsMetadata, ...] = field(default=(), kw_only=True, compare=False, repr=False). This preserves all 13 old positional fields and __match_args__, permits old calls unchanged, and preserves equality/repr over the old fields. The new optional keyword and canonical annotations change the introspected signature. dataclasses.asdict/astuple/fields include the additional metadata; this serialization difference is intentional and must be characterized. New records live under project_model.__module__; the old import paths remain aliases to those same classes. No __module__ or pickle shim is authorized. Existing manifests remain unhashable when their raw dictionaries participate, as before. The aggregate GameMakerResourceModels.project intentionally changes type/shape; it gets no legacy name/resource_count properties.

The new option_files tuple follows the current traversal order and contains every successfully decoded object-root option file, including empty objects and unknown-only metadata. Flatten each metadata record's existing option objects into manifest.options; do not parse them twice. Ignore option_files in existing equality/repr specifically to preserve the old comparison surface, not to imply its raw metadata was discarded.

Add only these derived properties: GameMakerProjectManifest.audio_groups_state returns missing/malformed/unnamed/available using the current raw AudioGroups lookup and cached parsed names; ProjectAudioGroup.initial_loaded preserves the old default-name and first-bool policy; ProjectAudioGroup.gain preserves the old string/int/float conversion and TypeError/ValueError fallback. They compute at consumer access time. In particular, huge-int OverflowError, nonfinite values and mutable raw changes must not move to manifest construction time.

## Parser and side-effect contract

project_manifest.py exposes parse_project_options_document(document: GameMakerJsonDocument, *, platform: str) -> ProjectOptionsMetadata | None. It only checks the object root, selects case-sensitive option_ keys in insertion order, and constructs source locations with the current first-substring line lookup. It emits no logs/diagnostics and performs no file I/O. The model leaf must not import GameMakerJsonDocument or its decoder.

Replace the manifest's two _read_lenient_json_file callers and delete that legacy-return adapter. The small project-service reader _read_project_document(path: str) -> GameMakerJsonDocument | None preserves the existing read/decode catch exactly: OSError, JSONDecodeError, TypeError, ValueError; RecursionError and other BaseExceptions retain propagation. The main YYP caller still owns malformed-root diagnostics; options still silently omit malformed/nonobject files. _parse_project_options keeps its acquisition responsibility and name, returning tuple[ProjectOptionsMetadata, ...] to its sole loader caller; the loader flattens those same option records before the existing group parsing. Canonical JsonObject/JsonValue must be used across every project/config/group/options parser. Remove legacy JsonDict/JsonList imports and narrowing casts from this owner; retain object only for the existing missing-path sentinel where necessary.

Manifest acquisition keeps its recursive sorted walk, case-sensitive .yy suffix, path containment/validation, platform derivation and warning order. Settings keeps its different ordered platform-directory scan, case-insensitive suffix, candidate collection and source-rejection behavior. Do not unify these acquisition policies.

In ProjectSettingsConverter._project_options_source_result, keep UTF-8 open/read inside its existing OSError handler and outside the JSON parse handler. Then call R10 parse_gamemaker_json with the original source/path and the pure options parser using the existing _platform_from_options_path helper. Preserve malformed root/JSON skipped results, I/O failed results, propagated UnicodeDecodeError and RecursionError, exact reason strings, and the loaded-options fallback when no candidates exist. Validation happens on every operation.

Fresh valid metadata is a validation result only. update_project_settings must still render cached manifest.options, even when valid option values on disk have changed since load; it must not replace the manifest or options tuple. Replacing a file with an empty object can therefore still validate while old options render. Cancellation, early reasons, logging, missing project.godot checks, writes and return timing remain in their current methods. _audio_groups_source_result first retains the existing manifest-availability result, then maps the new specific state to the same operation reason/result.

In AssetRegistryConverter._audio_group_registry, replace only the loaded and gain expressions with the two model properties, in their existing evaluation order. Delete the sole _audio_group_initial_loaded helper. Keep _metadata_float and every other family consumer. Retain default group insertion, last duplicate replacement, sorted final group output and sound attachment order.

Delete resource_models.ProjectModel and _project_model. Set GameMakerResourceModels.project directly to the manifest already loaded by parse_gamemaker_resource_models, preserving identity. Change the three obsolete assertions in tests/test_conversion_architecture.py to project_name/len(resources); add identity proof in the new consumer test owner. Other family parsing and the descriptive resource_models reader remain deferred.

## Callers, dependencies and non-goals

The fresh caller inventory still finds 17 production manifest importers. Only the four existing production implementation paths project_manifest, project_settings, asset_registry and resource_models change, plus the new project_model owner. Constructors occur in the manifest loader/parsers, tests/test_base_converter.py and tests/test_diagnostic_models.py; the latter exercises two positional constructor arguments. R11-current-caller-contract-evidence.json records their source/hash/signature/field inventory.

The source review found no project-record asdict/astuple/pickle/__module__/signature/class-pattern consumer. Existing reflection sites belong to diagnostics, GML contracts, receipt tooling or unrelated size evidence; the parity harness's generated asdict call serializes diagnostic issues. No manifest persistence compatibility is inferred from this absence. Characterize the declared signature/module/serialization differences explicitly instead of claiming complete binary/API identity.

Dependency direction: consumers -> project_manifest API -> project_model; project_manifest -> gamemaker_json/json_values and project_source_paths; project_model -> json_values and diagnostic_models plus standard library. Existing discovery -> manifest -> source_paths stays acyclic. No model dependency on parser, filesystem resolution, converters, renderers or writers.

RoomOrderNodes remains raw under R20: resource_index.GameMakerResourceIndex._apply_yyp_room_order and asset_registry.AssetRegistryConverter._room_order_indices have different filtering, fallback and exception timing. Exact current locations are resource_index.py:773 and asset_registry.py:3920; use qualified symbols after imports shift. No eager room-order validation or migration is allowed in R11. R11 retains resource-array traversal and order metadata semantics; R26 owns final raw-alias enforcement. No other family models, native transaction changes, R10 decoder changes, packaging or release/version changes.

## Allowed files and size acceptance

Exactly nine implementer paths: src/conversion/project_model.py (new); src/conversion/project_manifest.py; src/conversion/project_settings.py (two source-result methods/imports only); src/conversion/asset_registry.py (two audio expressions/sole helper deletion only); src/conversion/resource_models.py (duplicate project projection removal only); tests/test_project_model.py (new); tests/test_project_model_consumers.py (new); tests/test_conversion_architecture.py (three obsolete assertions only); maintainability-baseline.json (actual reductions only).

Root owns contracts/ledger, architecture-verification.json and coverage-policy.json integration and any final documentation mapping. No shared-manifest or coverage edits by the implementer. Existing affected consumer test sources remain unchanged except the three specified obsolete assertions.

Current preliminary physical/structural sizes: manifest 1091/1984, settings 718/1476, registry 4138/6881, resource_models 602/1035. The eight record bodies/methods/factory/path helper total 107 physical/240 structural units before imports/decorators/separators. Refresh all values on accepted R04 source. New project_model <=500 physical and <=650 structural; each new test owner <=1500 physical/structural; new functions <=150 physical/structural, C15, nesting4, parameters8 (test functions <=200 physical/structural). Existing changed owners/functions may not grow debt. No allowance, exclusion, suppression or threshold change; measure properties and destination imports with the final policy. This is a decomposition/removal claim only for actual record and duplicate owner removal, not import formatting.

## Characterization, proof and completion

Characterize on immutable accepted base first: all eight constructor/default/frozen/equality/repr/raw-reference contracts; positional and keyword-only/matching behavior; module/serialization changes; separate empty factories; shared nested option-value identity and raw mutation versus cached tuple behavior; metadata unknown fields and source text/path; repeated/duplicate option traversal; original source lines; malformed/missing/null/unknown cases; resource order coercions including OverflowError; config/target/group quirks; audio access-time gain/load behavior. Use the existing 46 outcomes as a bounded starting inventory, not an exhaustive claim.

Through real settings and registry consumers, prove fresh validation versus cached output after valid replacement, empty/nonobject/encoding/I/O changes, uppercase .YY, no-candidate injected fallback, cancellation/log/error timing and no-write results. Run existing native source containment cases and real generated Godot settings/audio outputs. Preserve the deferred room-order consumers and their existing tests unchanged. Architecture assertions enforce one record owner, eight direct identities, the canonical project boundary and the exact deleted duplicates; they must not falsely assert that all family raw reads are gone.

Run native Pyright --warnings 0/0, project/tracked Ruff with final R04 layout, focused tests after fixes, the strict Git-parent debt/diff gate, then frozen full unittest with exact Godot 4.7.2.stable.official.ed1daf0bf and all five pinned external projects. Match existing Linux/Windows source-safety and native receipt jobs; required proofs have zero skips, all ordinary full-suite skips are classified. Root serializes exact required test IDs and coverage cohort updates with unchanged floors before integration.

Performance proof is required for both shared manifest/discovery and the newly shared fresh settings-validation operation. Reuse the frozen R10 five-fixture 201-file/174499-byte inventory and workload. Add a separately named workload that loads/injects each manifest before timing, then calls each fixture's Windows _project_options_source_result once per pass, three passes per measured process; freeze exact fixture/state/candidate-file inventories and record skipped/completed/failed results. The candidate now constructs option records/source lines during this formerly root-only validation, so report its measured cost separately. One warm-up then five isolated samples per side per workload, alternate immutable base/candidate with identical inputs, reserve CPU window, retain all samples/median/standard deviation/native peak RSS. No caching or validation weakening to meet a guessed target.

Run immutable five-fixture/all-14-field base/candidate parity and candidate/same-ref control using the accepted R01 contract and preserved 44 facade exports. Freeze code hashes before full/parity/performance proof and review.

Implementation owner: one designated isolated owner assigned by root after acceptance. Proposal owner: audit_gml_resources. Independent contract/code reviewer: audit_policy_tests_docs, then root actual-code/contract review. This document itself is not independent approval.

Completion: root accepts these exact API choices and nine paths at frozen C for isolated implementation and confirms the actual integrated R04 cleanup base before PR/integration; scoped consumers use the canonical project model/parser; named duplicates and legacy manifest adapter are deleted; no new debt/casts/suppressions; required characterization/native/full/parity/performance evidence is tied to frozen code; independent and root APPROVE; exact integration verified. No temporary R11 adapter is proposed. The eight manifest type exports are permanent API aliases; remaining family raw consumers and the descriptive resource_models reader remain explicitly owned by family tasks/R26.
