# Bound method context fixture

Minimal GameMaker LTS 2026 project used by `tests.test_bound_method_context_godot`.
It covers invocation-time `other`, nested and `method_call` dispatch, array
callbacks, direct and rebound script references, and unbound/rebound
constructors without third-party assets.
